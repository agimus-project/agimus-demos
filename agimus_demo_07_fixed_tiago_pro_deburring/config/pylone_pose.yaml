---
# Pylone pose in base_link frame.
# Default values match the simulation setup (pylone on table at x=1.5).
# On the real robot, overwrite with scripts/localize_pylone.py output.

pylone_x: 0.7
pylone_y: 0.0
pylone_z: 0.8 # 0.3 to ground
pylone_quat: [0.0, 0.0, 0.0, 1.0]
